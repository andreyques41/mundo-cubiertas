# Base de datos
POSTGRES_USER=mundocubiertas
POSTGRES_PASSWORD=mundocubiertas_dev
POSTGRES_DB=mundo_cubiertas
DATABASE_URL="postgresql://mundocubiertas:mundocubiertas_dev@localhost:5432/mundo_cubiertas"

# Backend (NestJS)
PORT=3000
NODE_ENV=development

# Frontend (Next.js) — variables publicas deben empezar con NEXT_PUBLIC_
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WHATSAPP_NUMBER=50612345678

# Fase 2 — Auth panel admin (NextAuth)
NEXTAUTH_SECRET=cambia_esto_por_una_clave_larga_y_aleatoria
NEXTAUTH_URL=http://localhost:3001
ADMIN_PASSWORD=cambia_esto_por_una_contrasena_segura
# WIP: ADMIN_EMAIL — se agrega cuando haya email corporativo y multi-usuario con roles

# Fase 2 — Storage (planos y PDFs)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=

# Fase 2 — Email
# RESEND_API_KEY=

# Fase 3 — WhatsApp Business API
# WHATSAPP_API_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
